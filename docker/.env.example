# AWS credentials
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=

# Cognito (from terraform output or AWS console)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=

# SQS test queue URLs (from terraform output after deploying messaging stack)
NOTIFICATIONS_QUEUE_URL=
BILLING_QUEUE_URL=

# SMTP (SES credentials)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
SMTP_FROM=no-reply@travelhub.local
