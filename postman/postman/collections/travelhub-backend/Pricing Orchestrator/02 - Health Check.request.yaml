$kind: http-request
name: Health Check
url: "{{base_url}}/pricing-orchestator/api/Health"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Health check returns 200', function () {
        pm.response.to.have.status(200);
      });
    language: text/javascript
order: 2000
